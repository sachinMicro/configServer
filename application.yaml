eureka:
  client:
    serviceUrl:
       defaultZone: http://HEGITD000266L.lnjb.local:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
    
spring:
   datasource:
     url: jdbc:mysql://localhost:3306/db_test
     username: root
     password: root
     driver-class: com.mysql.cj.jdbc.Driver
   jpa:
     database-platform: org.hibernate.dialect.MySQLDialect
     hibernate:
       ddl-auto: update
